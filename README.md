# zt10001.github.io

Personal website for Tao Zheng, built with Jekyll and a custom
terminal-window theme (`_sass/terminal.scss`), styled after
[oddbit.ai](https://oddbit.ai).

## Run locally

```bash
bundle install
bundle exec jekyll serve
```

GitHub Actions builds and deploys the site to GitHub Pages on pushes to
`master`.
